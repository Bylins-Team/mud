# ISSUE-005: Мёртвый/закомментированный код

**Тип**: Code Quality
**Критичность**: LOW
**Файл**: `src/administration/accounts.cpp:140-159`
**Статус**: Активна

## Описание

Большой блок функционального кода (20 строк) закомментирован, но оставлен в продакшене:

```cpp
/*      if (line.starts_with("hl: ")) {
            utils::Split(tmp, line);
            login_index tmp_li;
            tmp_li.count = atoi(tmp[2].c_str());
            tmp_li.last_login = atoi(tmp[3].c_str());
            this->history_logins.insert(std::pair<std::string, login_index>(tmp[1].c_str(), tmp_li));
        }
        if (line.starts_with("p: ")) {
            // ... еще 15 строк
        }
*/
```

Это создаёт проблемы:
- Затрудняет понимание логики - неясно, работает эта функциональность или нет
- Может содержать устаревший/небезопасный код
- Создаёт путаницу о том, какая функциональность активна
- Увеличивает размер кода без пользы

## Воспроизведение

1. Чтение файла `Account::read_from_file()`
2. Строки с префиксами "hl:", "p:", "Pwd:", "ll:" игнорируются
3. Функциональность истории логинов не работает
4. Данные не загружаются, но файлы могут содержать эти записи

## Решение

1. **Если код не нужен** - удалить его полностью (git сохранит историю)
2. **Если код нужен** - раскомментировать, протестировать и убедиться в корректности
3. **Если требуется доработка** - создать TODO/FIXME с описанием проблемы

```cpp
// Правильный подход - либо код работает:
if (line.starts_with("hl: ")) {
    // Рабочий код
}

// Либо удалён полностью (история в git)
```
